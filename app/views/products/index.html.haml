- cache ['homepage_tops', @hit, @newest, @homepage] do
  .jumbotron
    %h2 Хиты продаж
    %hr/
    .row
      = render collection: @hit.includes(:images), partial: 'products/top_card', as: :product, locals: { css: 'label-success', name: 'Хит продаж' }
  .jumbotron
    %h2 Новинки
    %hr/
    .row
      = render collection: @newest.includes(:images), partial: 'products/top_card', as: :product, locals: { css: 'label-danger', name: 'Новинка' }
  .jumbotron
    %h2 Популярное
    %hr/
    .row
      = render collection: @homepage.includes(:images), partial: 'products/top_card', as: :product, locals: { css: 'label-info', name: 'Популярное' }
.well.well-sm
  = Conf['ck.homepage_seo'].html_safe
