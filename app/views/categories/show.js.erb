(function(){
  var partial = "<%= escape_javascript(render 'filters_and_results', filters: @category.category_filters.includes(filter: :values), brands: @category.brands, products: @products) %>"
  $('#filters_and_results').html(partial);
})();
