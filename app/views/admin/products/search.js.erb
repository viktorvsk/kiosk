$('[data-catalog-products]').html("<%= escape_javascript(render(@products)) %>");<%#  %>
$('[data-catalog-products-paginate]').html("<%= escape_javascript(paginate(@products, format: :js, remote: true)) %>");
Kiosk.BindingManager.init();
