(function () {
  var template = "<%= escape_javascript(render partial: 'admin/categories/filter', collection: @category.category_filters.sort_by(&:position), as: :filter) %>";
  $('#filters').html(template);
  window.Kiosk.CategoryFiltersManager.init();
}());
