<% if @vendor_product.reload.active? %>
  $('[data-vendor-product-id="<%= escape_javascript(@vendor_product.id.to_s) %>"]').removeClass("inactive");
<% else %>
  $('[data-vendor-product-id="<%= escape_javascript(@vendor_product.id.to_s) %>"]')
    .addClass("inactive");
<% end %>

