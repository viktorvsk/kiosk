%tr
  %td
    %div{ 'data-draggable' => 'vendorProduct', 'data-vendor-product-id' => product.id }
      = product.name
      %hr/
      %b= "#{product.price} грн."
      %i= product.merchant.name
      - unless product.active?
        %b Неактивный
