%li{ 'data-draggable' => 'vendorProduct', 'data-vendor-product-id' => product.id }
  %span
    = product.name
    %small= product.id
  %hr/
  %span
    %b= "#{product.price} грн."
    %i= product.merchant.name
    - unless product.active?
      %b Неактивный
    - if product.is_rrc?
      %b RRC
