.page-header
  = link_to admin_vendor_merchants_path, :class => 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_admin_vendor_merchant_path(@admin_vendor_merchant), :class => 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show admin_vendor_merchant

%dl.dl-horizontal
