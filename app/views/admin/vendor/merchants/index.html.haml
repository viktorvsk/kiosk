.page-header
  = link_to new_admin_vendor_merchant_path, :class => 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    Новый поставщик
  %h1= @merchants.present? ? 'Все поставщики' : 'Поставщики отсутствуют'

- if @merchants.present?
  .row
    = render @merchants


