.page-header
  %h1 Новая характеристика
.row
  = form_for(@property, url: admin_properties_path) do |f|
    %ul.list-group
      %li.list-group-item= field_for(f, :name)
    = f.submit 'Отправить', class: 'btn btn-success'
